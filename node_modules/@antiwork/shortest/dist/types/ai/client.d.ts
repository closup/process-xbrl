import Anthropic from '@anthropic-ai/sdk';
import { AIConfig } from '../types/ai';
import { BrowserTool } from '../browser/core/browser-tool';
export declare class AIClient {
    private client;
    private model;
    private maxMessages;
    private debugMode;
    constructor(config: AIConfig, debugMode?: boolean);
    processAction(prompt: string, browserTool: BrowserTool, outputCallback?: (content: Anthropic.Beta.Messages.BetaContentBlockParam) => void, toolOutputCallback?: (name: string, input: any) => void): Promise<{
        messages: Anthropic.Beta.Messages.BetaMessageParam[];
        finalResponse: Anthropic.Beta.Messages.BetaMessage;
    } | undefined>;
    makeRequest(prompt: string, browserTool: BrowserTool, outputCallback?: (content: Anthropic.Beta.Messages.BetaContentBlockParam) => void, toolOutputCallback?: (name: string, input: any) => void): Promise<{
        messages: Anthropic.Beta.Messages.BetaMessageParam[];
        finalResponse: Anthropic.Beta.Messages.BetaMessage;
    }>;
}
