import { BrowserToolInterface } from '../../types/browser';
export declare class GitHubTool {
    private totpSecret;
    private readonly selectors;
    constructor(secret?: string);
    private validateSecret;
    generateTOTPCode(): {
        code: string;
        timeRemaining: number;
    };
    GithubLogin(browserTool: BrowserToolInterface, credentials: {
        username: string;
        password: string;
    }): Promise<{
        success: boolean;
        error?: string;
    }>;
}
