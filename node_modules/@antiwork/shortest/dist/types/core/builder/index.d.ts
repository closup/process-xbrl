import type { TestFunction } from '../../types';
export declare class TestBuilder {
    static generatePrompt(test: TestFunction): string;
    static parseModule(compiledModule: any): Promise<TestFunction[]>;
}
