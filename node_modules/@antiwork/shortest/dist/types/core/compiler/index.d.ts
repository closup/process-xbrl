export declare class TestCompiler {
    private cacheDir;
    private defaultOptions;
    constructor();
    compileFile(filePath: string): Promise<string>;
    loadModule(filePath: string, cwd: string): Promise<any>;
}
