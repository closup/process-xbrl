export declare class TestRunner {
    private config;
    private cwd;
    private exitOnSuccess;
    private forceHeadless;
    private targetUrl;
    private compiler;
    private browserManager;
    private logger;
    private debugAI;
    constructor(cwd: string, exitOnSuccess?: boolean, forceHeadless?: boolean, targetUrl?: string, debugAI?: boolean);
    initialize(): Promise<void>;
    private findTestFiles;
    private executeTest;
    private executeTestFile;
    runFile(pattern: string): Promise<void>;
    runAll(): Promise<void>;
}
