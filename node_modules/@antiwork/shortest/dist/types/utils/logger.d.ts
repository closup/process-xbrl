import { AssertionError } from '../types';
export type TestStatus = 'pending' | 'running' | 'passed' | 'failed';
export declare class Logger {
    private currentFile;
    private testResults;
    private startTime;
    startFile(file: string): void;
    reportTest(name: string, status?: TestStatus, error?: Error): void;
    private getStatusIcon;
    summary(): void;
    allTestsPassed(): boolean;
    reportStatus(message: string): void;
    error(context: string, message: string): void;
    reportError(context: string, message: string): void;
    reportAssertion(step: string, status: 'passed' | 'failed', error?: AssertionError): void;
}
